query cloudcmsGetAllPagesByTemplateName(
  $filter: String!
  $take: Int
  $skip: Int
  $sort: String
  $direction: String
) {
  ag_pages(
    filter: $filter
    take: $take
    skip: $skip
    sort: $sort
    direction: $direction
  ) {
    pageID
    title
    templateName
  }
}